import { ShowWhenEventDirective } from './show-when-event.directive';

describe('ShowWhenEventDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowWhenEventDirective();
    expect(directive).toBeTruthy();
  });
});
